// Copy this file to analytics.config.js and set one provider.
// The app auto-loads analytics.config.js at runtime if present.

window.PORTFOLIO_ANALYTICS = {
  provider: 'plausible',
  domain: 'your-domain.com'
};

// GA4 alternative:
// window.PORTFOLIO_ANALYTICS = {
//   provider: 'ga4',
//   measurementId: 'G-XXXXXXXXXX'
// };
